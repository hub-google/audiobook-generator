name: Audiobook Automation Pipeline (Parallel)

on:
  workflow_dispatch:
    inputs:
      catalog_url:
        description: '小說目錄網址 (e.g. https://tw.hjwzw.com/Book/Chapter/1644)'
        required: true
        default: 'https://tw.hjwzw.com/Book/Chapter/1644'
      start_chap:
        description: '開始章節 (Start Chapter, 1-based)'
        required: true
        default: '1'
      end_chap:
        description: '結束章節 (End Chapter, 1-based)'
        required: true
        default: '10'
      exclude_chapters:
        description: '排除章節的編號 (逗號分隔，如 12,105,106)'
        required: false
        default: ''
      chapters_per_worker:
        description: '每台工作機負責的章節數 (預設 10，最多 20 台並行)'
        required: true
        default: '10'
      zip_password:
        description: 'ZIP 解密密碼 (可選，從 GUI 或 Secrets 傳入)'
        required: false
        default: ''
  schedule:
    # GitHub cron uses UTC. This checks every 30 minutes and re-runs only the
    # failed jobs from the latest manually-started audiobook run.
    - cron: '*/30 * * * *'

permissions:
  contents: write
  actions: write

# Prevent a scheduled retry from overlapping the active audiobook run.
concurrency:
  group: youtube-resumable-upload
  cancel-in-progress: false

jobs:
  retry_failed_run:
    name: "Retry latest failed audiobook run"
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest

    steps:
      - name: Re-run failed jobs after the 30-minute check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY: ${{ github.repository }}
          WORKFLOW_FILE: audiobook.yml
        run: |
          latest_run=$(gh run list \
            --repo "$REPOSITORY" \
            --workflow "$WORKFLOW_FILE" \
            --event workflow_dispatch \
            --limit 1 \
            --json databaseId,status,conclusion,updatedAt \
            --jq '.[0] // {}')

          run_id=$(jq -r '.databaseId // empty' <<< "$latest_run")
          status=$(jq -r '.status // empty' <<< "$latest_run")
          conclusion=$(jq -r '.conclusion // empty' <<< "$latest_run")
          updated_at=$(jq -r '.updatedAt // empty' <<< "$latest_run")

          # Fetch run_attempt to prevent infinite retry loops
          run_attempt=""
          if [ -n "$run_id" ]; then
            run_attempt=$(gh run view "$run_id" --repo "$REPOSITORY" --json attempt --jq '.attempt // 1' 2>/dev/null || echo "1")
          fi

          echo "## Automatic retry check" >> "$GITHUB_STEP_SUMMARY"
          if [ -z "$run_id" ]; then
            echo "No manually-started audiobook run was found." >> "$GITHUB_STEP_SUMMARY"
          elif [ "$status" != "completed" ]; then
            echo "Run [$run_id](https://github.com/$REPOSITORY/actions/runs/$run_id) is still $status; no duplicate run was started." >> "$GITHUB_STEP_SUMMARY"
          elif [ "$conclusion" = "failure" ]; then
            # Stop retrying after 4 attempts to prevent infinite cron loops
            if [ "${run_attempt:-1}" -ge 4 ]; then
              echo "⛔ Run [$run_id](https://github.com/$REPOSITORY/actions/runs/$run_id) has already been retried $run_attempt times. **Max retries (4) exceeded — manual intervention required.**" >> "$GITHUB_STEP_SUMMARY"
            else
              updated_epoch=$(date -d "$updated_at" +%s)
              now_epoch=$(date -u +%s)
              age_seconds=$((now_epoch - updated_epoch))
              if [ "$age_seconds" -lt 1800 ]; then
                remaining_minutes=$(((1800 - age_seconds + 59) / 60))
                echo "Run [$run_id](https://github.com/$REPOSITORY/actions/runs/$run_id) failed less than 30 minutes ago (attempt $run_attempt). It will be eligible in about $remaining_minutes minute(s)." >> "$GITHUB_STEP_SUMMARY"
              else
                gh run rerun "$run_id" --failed --repo "$REPOSITORY"
                echo "Re-running failed jobs from [$run_id](https://github.com/$REPOSITORY/actions/runs/$run_id) (attempt $run_attempt → $((run_attempt+1))). Existing chapter and YouTube checkpoints will be reused." >> "$GITHUB_STEP_SUMMARY"
              fi
            fi
          else
            echo "Latest manual run [$run_id](https://github.com/$REPOSITORY/actions/runs/$run_id) concluded with **$conclusion**; no retry is needed." >> "$GITHUB_STEP_SUMMARY"
          fi

  setup:
    name: "🔍 Setup — Parse Catalog & Build Matrix"
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.gen_matrix.outputs.matrix }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python Dependencies
        run: |
          for attempt in 1 2 3; do
            echo "Python dependency install attempt $attempt/3"
            if python -m pip install --upgrade pip && pip install -r requirements.txt; then
              break
            fi
            if [ "$attempt" -eq 3 ]; then exit 1; fi
            sleep 30
          done

      - name: Parse Catalog & Generate config.yaml + matrix.json
        run: |
          for attempt in 1 2 3; do
            echo "Catalog parsing attempt $attempt/3"
            if python src/catalog_parser.py \
              --url "${{ github.event.inputs.catalog_url }}" \
              --start ${{ github.event.inputs.start_chap }} \
              --end   ${{ github.event.inputs.end_chap }} \
              --output config.yaml \
              --workers ${{ github.event.inputs.chapters_per_worker }} \
              --matrix-output matrix.json \
              --exclude-chapters "${{ github.event.inputs.exclude_chapters }}"; then
              break
            fi
            if [ "$attempt" -eq 3 ]; then exit 1; fi
            sleep 60
          done

      - name: Export matrix to GitHub Actions output
        id: gen_matrix
        run: |
          echo 'matrix<<EOF' >> $GITHUB_OUTPUT
          cat matrix.json >> $GITHUB_OUTPUT
          echo "" >> $GITHUB_OUTPUT
          echo 'EOF' >> $GITHUB_OUTPUT

      - name: Generate Setup Summary
        run: |
          echo "## 📚 Audiobook Pipeline Started" >> "$GITHUB_STEP_SUMMARY"
          echo "✅ **Target URL:** ${{ github.event.inputs.catalog_url }}" >> "$GITHUB_STEP_SUMMARY"
          echo "✅ **Chapter Range:** ${START} ~ ${END}" >> "$GITHUB_STEP_SUMMARY"
          echo "✅ **Workers Created:** $(jq '. | length' matrix.json)" >> "$GITHUB_STEP_SUMMARY"
        env:
          START: ${{ github.event.inputs.start_chap }}
          END: ${{ github.event.inputs.end_chap }}

      - name: Upload config.yaml as shared artifact
        uses: actions/upload-artifact@v4
        with:
          name: shared-config
          path: config.yaml

# ─────────────────────────────────────────────────────────────
# Worker Pipeline: 並行獨立處理各 Worker 的章節 (一條龍 End-to-End)
# ─────────────────────────────────────────────────────────────
  process_chapters:
    name: "⚡ Worker ${{ matrix.worker_id }} (Ch ${{ matrix.start_chap }}~${{ matrix.end_chap }})"
    needs: setup
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 20
      matrix: ${{ fromJSON(needs.setup.outputs.matrix) }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install System Deps (FFmpeg + CJK Fonts)
        run: |
          for attempt in 1 2 3; do
            echo "System dependency install attempt $attempt/3"
            if sudo env DEBIAN_FRONTEND=noninteractive timeout 10m apt-get update -qq -o Acquire::Retries=3 && \
              sudo env DEBIAN_FRONTEND=noninteractive timeout 15m apt-get install -y --no-install-recommends -o Acquire::Retries=3 -o DPkg::Lock::Timeout=60 ffmpeg fonts-noto-cjk fonts-wqy-zenhei; then
              break
            fi
            if [ "$attempt" -eq 3 ]; then exit 1; fi
            sleep 30
          done

      - name: Install Python Dependencies
        run: |
          for attempt in 1 2 3; do
            echo "Python dependency install attempt $attempt/3"
            if pip install -r requirements.txt; then break; fi
            if [ "$attempt" -eq 3 ]; then exit 1; fi
            sleep 30
          done

      - name: Download shared config
        uses: actions/download-artifact@v4
        with:
          name: shared-config

      - name: Restore Worker Workspace Checkpoint
        uses: actions/cache/restore@v4
        with:
          path: Workspace/
          key: ${{ matrix.book_title }}-chap${{ matrix.start_chap }}-${{ matrix.end_chap }}-worker${{ matrix.worker_id }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            ${{ matrix.book_title }}-chap${{ matrix.start_chap }}-${{ matrix.end_chap }}-worker${{ matrix.worker_id }}-

      - name: Log Cache Location
        run: |
          echo "=================================================="
          echo "📂 斷點續傳快取路徑 (Cache Location):"
          echo "   • 虛擬機工作目錄: Workspace/"
          echo "   • 雲端快取管理頁: https://github.com/${{ github.repository }}/actions/caches"
          echo "=================================================="

      - name: Run Per-Chapter End-to-End Pipeline (Real-Time Saving)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          for attempt in 1 2 3; do
            echo "Worker pipeline attempt $attempt/3"
            if python src/worker_pipeline.py --stage pipeline --worker-id ${{ matrix.worker_id }} --batch-size 1; then
              exit 0
            fi
            if [ "$attempt" -eq 3 ]; then
              echo "❌ Worker failed after 3 attempts. The checkpoint will be saved and this run will remain failed." >> "$GITHUB_STEP_SUMMARY"
              exit 1
            fi
            echo "⚠️ Worker attempt $attempt failed; retrying in 60 seconds. Existing Workspace files will be reused." >> "$GITHUB_STEP_SUMMARY"
            sleep 60
          done

      - name: Save Worker Workspace Checkpoint
        if: always()
        uses: actions/cache/save@v4
        with:
          path: Workspace/
          key: ${{ matrix.book_title }}-chap${{ matrix.start_chap }}-${{ matrix.end_chap }}-worker${{ matrix.worker_id }}-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Upload MP4 Videos & Subtitles Only (Lightweight Artifact for YouTube Upload)
        uses: actions/upload-artifact@v4
        with:
          name: mp4-worker-${{ matrix.worker_id }}
          path: |
            Workspace/*/Video/
            Workspace/*/Subtitles/
            Upload_Subtitles/
          if-no-files-found: warn

      - name: Upload full worker workspace artifact (Heavyweight for Inspection)
        uses: actions/upload-artifact@v4
        with:
          name: video-worker-${{ matrix.worker_id }}
          path: Workspace/
          if-no-files-found: warn

      - name: Worker Job Summary
        if: always()
        timeout-minutes: 5
        env:
          WORKER_ID: ${{ matrix.worker_id }}
          START_CHAP: ${{ matrix.start_chap }}
          END_CHAP: ${{ matrix.end_chap }}
          JOB_STATUS: ${{ job.status }}
        run: |
          if [ ! -f "config.yaml" ]; then
            echo "## Worker $WORKER_ID Pipeline Status" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "- Overall: **FAILED**" >> "$GITHUB_STEP_SUMMARY"
            echo "- Job status: **$JOB_STATUS**" >> "$GITHUB_STEP_SUMMARY"
            echo "- Resume point: **setup/download config**" >> "$GITHUB_STEP_SUMMARY"
            echo "- Reason: config.yaml is unavailable; inspect the first failed setup step." >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          python - <<'PY'
          import os
          import yaml
          from src.pipeline_checkpoint import PipelineCheckpoint

          with open("config.yaml", encoding="utf-8") as config_file:
              config = yaml.safe_load(config_file)
          book_title = config["book_title"]
          workspace = os.path.abspath(os.path.join(config["paths"]["workspace_base"], book_title))
          worker_id = int(os.environ["WORKER_ID"])
          per_worker = int(config["chapters_per_worker"])
          start = worker_id * per_worker
          chapters = config["selected_indices"][start:start + per_worker]
          checkpoint = PipelineCheckpoint(
              workspace, book_title, worker_id, chapters
          )
          incomplete = checkpoint.incomplete_chapters()
          print("[RUN_SUMMARY] " + __import__("json").dumps({
              "kind": "worker", "worker": worker_id,
              "completed": len(chapters) - len(incomplete), "total": len(chapters),
              "incomplete": incomplete,
          }, ensure_ascii=False))
          with open(os.environ["GITHUB_STEP_SUMMARY"], "a", encoding="utf-8") as summary:
              summary.write(checkpoint.markdown_summary())
          PY

# ─────────────────────────────────────────────────────────────
# Automatic YouTube API Upload & Playlist Builder (無直播，純 API 暴速上傳)
# ─────────────────────────────────────────────────────────────
  upload_to_youtube:
    name: "📤 Automatic YouTube API Upload (Video + Subtitles + Playlist)"
    needs: process_chapters
    # Run even if some workers failed — the uploader will work with
    # whatever chapter artifacts are available.  Only skip if the entire
    # matrix was cancelled (no artifacts at all).
    if: always() && needs.process_chapters.result != 'cancelled'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python Dependencies
        run: |
          for attempt in 1 2 3; do
            echo "Python dependency install attempt $attempt/3"
            if pip install -r requirements.txt; then break; fi
            if [ "$attempt" -eq 3 ]; then exit 1; fi
            sleep 30
          done

      - name: Install FFmpeg
        run: |
          for attempt in 1 2 3; do
            echo "FFmpeg install attempt $attempt/3"
            if sudo env DEBIAN_FRONTEND=noninteractive timeout 10m apt-get update -qq -o Acquire::Retries=3 && \
              sudo env DEBIAN_FRONTEND=noninteractive timeout 10m apt-get install -y --no-install-recommends -o Acquire::Retries=3 -o DPkg::Lock::Timeout=60 ffmpeg; then
              break
            fi
            if [ "$attempt" -eq 3 ]; then exit 1; fi
            sleep 30
          done

      - name: Restore latest YouTube upload checkpoint
        uses: actions/cache/restore@v4
        with:
          path: |
            upload_resume_state/
            Workspace/*/Cover/
            Upload_Subtitles/
          key: youtube-upload-state-source-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            youtube-upload-state-source-${{ github.run_id }}-

      - name: Run YouTube API Uploader
        env:
          YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
          YOUTUBE_CLIENT_ID: ${{ secrets.YOUTUBE_CLIENT_ID }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          for attempt in 1 2 3; do
            echo "YouTube uploader attempt $attempt/3"
            set +e
            python src/youtube_api_uploader.py --run-id "${{ github.run_id }}" --repo "${{ github.repository }}" --privacy "public" --state-file upload_resume_state/state.json
            code=$?
            set -e

            if [ "$code" -eq 0 ]; then exit 0; fi
            if [ "$code" -eq 75 ]; then
              echo "⏸️ YouTube uploader requested a safe pause. The checkpoint is preserved; this run will retry after 30 minutes." >> "$GITHUB_STEP_SUMMARY"
              exit 75
            fi
            if [ "$attempt" -eq 3 ]; then
              echo "❌ YouTube upload failed after 3 attempts. The checkpoint will be saved and this run will remain failed." >> "$GITHUB_STEP_SUMMARY"
              exit "$code"
            fi
            echo "⚠️ YouTube upload attempt $attempt failed; retrying in 60 seconds from its saved state." >> "$GITHUB_STEP_SUMMARY"
            sleep 60
          done

      - name: Generate Job Summary
        if: always()
        run: |
          python - <<'PY'
          import json, os
          from src.publication_checkpoint import PublicationCheckpoint
          
          state_file = "upload_resume_state/state.json"
          if not os.path.exists(state_file):
              with open(os.environ["GITHUB_STEP_SUMMARY"], "a", encoding="utf-8") as f:
                  f.write("## YouTube Upload Status\nNo state file found.\n")
              exit(0)
              
          with open(state_file, encoding="utf-8") as f:
              state = json.load(f)
              
          done = len(state.get("completed_titles", []))
          total = len(state.get("part_plan", [])) or max(done, 1)
          pending = len(state.get("pending_thumbnails", {}))
          pending_playlist = len(state.get("pending_playlist", {}))
          pending_captions = len(state.get("pending_captions", {}))
          pending_publish = len(state.get("pending_publish", {}))
          status = state.get("status", "unknown")
          print("[RUN_SUMMARY] " + json.dumps({
              "kind": "youtube", "status": status, "completed": done, "total": total,
              "pending_thumbnails": pending, "pending_playlist": pending_playlist,
              "pending_captions": pending_captions, "pending_publish": pending_publish,
              "reason": state.get("reason"), "retry_at": state.get("retry_at"),
              "playlist_url": state.get("playlist_url"),
          }, ensure_ascii=False))
          
          status_icon = "⏸️"
          if status == "complete": status_icon = "🎉"
          elif status == "failed": status_icon = "❌"
          elif status == "unknown": status_icon = "⚠️"
          
          with open(os.environ["GITHUB_STEP_SUMMARY"], "a", encoding="utf-8") as f:
              f.write(f"## {status_icon} YouTube Upload Status: {status.upper()}\n\n")
              f.write(f"| 項目 | 狀態資訊 |\n|---|---|\n")
              f.write(f"| **任務進度** | **{done} / {total} Parts** ({done/total*100:.1f}%) |\n")
              f.write(f"| **Pending Thumbnails** | **{pending}** |\n")
              f.write(f"| **Pending Playlist Inserts** | **{pending_playlist}** |\n")
              f.write(f"| **Pending CC Captions** | **{pending_captions}** |\n")
              f.write(f"| **Pending Final Publish** | **{pending_publish}** |\n")
              if "reason" in state and state["reason"]:
                  f.write(f"| **Reason** | `{state['reason']}` |\n")
              if "retry_at" in state and state["retry_at"]:
                  f.write(f"| **Retry After** | `{state['retry_at']}` |\n")
                  
              playlist_url = state.get("playlist_url")
              if playlist_url and playlist_url.startswith("http"):
                  f.write(f"| **播放清單** | [點此開啟 YouTube 播放清單]({playlist_url}) |\n")
              f.write("\n")
              f.write(PublicationCheckpoint(state_file).markdown_summary())
          PY

      - name: Save YouTube upload checkpoint
        if: always() && hashFiles('upload_resume_state/state.json') != ''
        uses: actions/cache/save@v4
        with:
          path: |
            upload_resume_state/
            Workspace/*/Cover/
            Upload_Subtitles/
          key: youtube-upload-state-source-${{ github.run_id }}-${{ github.run_attempt }}
