# 章節選擇與重複判斷需求書

## 欄位型別與用途

| 欄位 | 型別 | 規則 |
|---|---|---|
| UUID | 正整數 | 網站目錄中的穩定章節索引，不因排序改變。 |
| 編號章節數 | 正整數 | 實際製作順序，永遠依序為 1、2、3……，不允許小數。 |
| 網站章節數正規化 | 正十進位數或空值 | 可由網站標題解析或由使用者覆寫；允許如 `1249`、`1249.5`、`1249.25`。 |
| 網站顯示章節數 | 字串或空值 | 忠實顯示網站標題中的章號，不因手動正規化值而改寫。 |
| 章節名稱 | 字串 | 網站章名或使用者修改後的章名。 |

開始章節、結束章節、排除章節與所有實際製作章號都必須維持整數。小數只適用於「網站章節數正規化」。

## 手動正規化章號

1. 使用者可輸入大於 0 的整數或一般十進位小數，不接受負數、零、指數表示法及非數字內容。
2. 數值以標準化字串永久保存，避免浮點誤差；例如 `1249.50` 保存及比較為 `1249.5`。
3. 留白代表刪除手動覆寫並恢復網站標題自動解析。
4. 即使網站標題解析不到章號、網站顯示章節數為空，仍須顯示並使用手動正規化值。
5. 手動值須一致套用於畫面顯示、重複判斷、正規化排序、每本書永久設定、雲端佇列及實際產出標題。
6. 依正規化章號排序時採十進位數值順序；排序後「編號章節數」仍重新編成連續整數。

## 重複判斷條件

使用者可勾選下列一項或多項；任一已勾選條件符合即視為重複：

1. 網站章節數正規化相同。
2. 章節名稱去除所有空白後相同。
3. 網站章節數正規化與去除空白後的章節名稱同時相同。

空的章號或空的章名不可彼此判定為重複。未勾選的條件不得影響結果。按下「套用」後必須立即重算。

## 重複群組顯示

1. 「只看重複章節」、表格最右側「重複」標記及重複章節明細彈框必須共用目前條件重算出的同一份結果。
2. 群組中的第一份與所有後續副本都須顯示「重複」。
3. 點擊群組中任何一筆的「重複」欄都能開啟明細。
4. 明細只顯示目前條件下互相關聯的完整群組，並顯示該群組目前符合的判斷條件。
5. 若不同已啟用條件形成連鎖關聯，明細顯示該連通群組的全部章節。
6. 「排除重複章節」只取消每組後出現的副本，保留第一份；顯示標記不得改變此規則。

## 小說封面生成需求

## 跨 Run 續作與還原順序

1. 每本書必須以正規化目錄網址產生的穩定 `book_profile_id` 作為書籍指紋；不得只靠書名、任務 ID 或模糊搜尋判定同一本書。
2. 雲端佇列必須按書籍指紋保存歷史 Run，派送新 Run 前必須選定並寫入唯一的 `artifact_source_run_id`，同時把該 ID 作為 `resume_source_run_id` 傳給 Workflow。Worker 不得自行掃描或猜測其他 Run。
3. 還原順序固定且不可交換：上一 Run 的 artifacts → GitHub Actions Cache → 重新製作。
4. Worker 必須先下載上一 Run 的 `shared-config` 與對應 `video-worker-N` artifact。只有 artifact 不存在，或經逐章 checkpoint 驗收確認不完整時，才允許讀取 Cache。
5. artifact 的 `shared-config.book_profile_id` 必須與本次 `config.yaml` 完全相同。若 artifact 存在但指紋不符、來源設定缺失或驗證損壞，流程必須明確失敗，不得靜默改用 Cache 或重新製作。
6. artifact 通過指紋及所有指定章節驗收後，該 Worker 必須跳過 Cache restore；後續 pipeline 只能重用已驗收檔案，不得重新爬取、清洗、TTS、產圖或合成影片。
7. artifact 只缺部分章節時，Cache 僅作第二順位補充；Cache 後仍缺失的章節才可重新製作。不得因部分缺失而重做已通過 artifact 驗收的章節。
8. Actions 日誌必須明確顯示鎖定的來源 Run ID、書籍指紋驗證結果、artifact 完整度，以及是否獲准進入 Cache fallback；artifact 下載或驗收失敗不得只留下無法察覺的 warning。
9. 自動化測試必須鎖定以上順序，至少涵蓋：同指紋選取最新 Run、不同指紋不得繼承、artifact 完整時 Cache 不執行、artifact 缺失或不完整時才執行 Cache，以及 pipeline 不再自行掃描歷史 Run。

### 通用熱門縮圖模板

1. 所有小說題材共用 `viral-v1` 固定構圖；小說差異只能填入題材、時代、衝突、人物、代表物件、巨大故事符號、色盤與光線等結構化變數。
2. Gemini 只負責根據可靠故事資料回傳 `visual_brief`，不得自行撰寫或改變最終構圖 Prompt；最終英文 Prompt 必須由 Python 固定模板組裝。
3. 主角必須是佔畫面主要比例的近距離半身人物，臉部清晰且位於下方標題區以上。輔助角色最多兩位，且視覺重要性不得超過主角。
4. 背景必須包含一個資料支持、可立即辨識的巨大故事符號。畫面必須高密度、有前中後景，不得使用遠景小人物、大片天空、大片空地、極簡構圖或乾淨留白文字區。
5. 畫面下方 35% 保留為文字覆蓋區：仍須有故事背景，但亮度、對比與細節稍低，不得形成空白底板。
6. 題材只影響故事內容與色盤；玄幻、都市、言情、末世、科幻、歷史與懸疑等題材都不得繞過固定構圖規則。
7. HF 只生成無文字底圖，禁止生成任何中英文字、數字、Logo、水印、簽名、邊框或 UI。
8. AI 主視覺與 Part 成品必須記錄模板版本。模板版本或文字設定改變時舊 AI 快取必須失效並重建；具有手動封面標記的使用者圖片不得因此被 AI 覆寫。

### 固定文字版型

1. 書名固定置於下方中央，最多兩行，依實際字寬自動縮放與均衡斷行。
2. 書名固定使用專案內 `fonts/YujiBoku.ttf`；缺少指定字型時流程必須停止，不得因作業系統不同自動更換書名字型。
3. 書名字效固定為黑色模糊陰影、近黑粗外框、暖金內框、象牙白字面。所有小說使用相同顏色與疊字次序。
4. 右上角固定顯示紅字白框的「全集」或「第N部」；左上角以小型白字顯示章節範圍與完結狀態。
5. 成品固定為 1280×720。不得為符合固定檔案容量而反覆降低 JPEG 品質；本地封面與手動封面不設 2 MB 上限。
6. 若外部發布平台自行拒絕檔案大小，必須回報平台錯誤，不得偷偷壓縮或降低畫質。
7. 圖片與快取紀錄必須先寫入暫存檔再原子替換，避免中途失敗留下可被誤判為完成的半成品。
