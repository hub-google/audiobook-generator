# 工作區工具路徑

- GitHub 命令列工具安裝於 `C:\Program Files\GitHub CLI\gh.exe`。
- 系統的 `PATH` 環境變數不一定包含 `gh.exe`；執行時一律使用上方的絕對路徑。

# 工作流程界線

- 封面預先檢查與小說抓取必須維持為彼此獨立的工作流程。
- `.github/workflows/cover-preflight.yml` 不得執行 `catalog_parser.py`、`crawler.py`，也不得連線抓取小說來源網站。
- 在封面預先檢查中，`catalog_url` 只能用來驗證書籍身分及產生固定不變的儲存索引鍵，不得因此送出網路請求。
- 封面預先檢查必須使用手動觸發時傳入的資料，以及已保存的書籍設定快照，建立所需的最小設定。它不得依賴章節清單、章節標題、選取矩陣或抓取程式產生的檔案。
- 修改工作流程前，必須先確認該流程需要的輸入、產生的檔案，以及後續使用這些結果的流程。不得只為取得少量資料，就呼叫範圍更大的工作流程。
- 修改封面、抓取、合併或發布行為時，必須同時測試變更的階段及其前後直接相鄰的階段。
- 如果現有測試沒有保障這次變更所影響的工作流程界線，必須新增防止問題再次發生的測試，才能視為完成變更。

# 純文字抓取程式失敗重跑規則

- 純文字抓取程式失敗後，必須使用 GitHub Actions 原生的 `rerun-failed-jobs` 功能重跑；不得將同一台執行機器內重試網路請求或重建連線狀態視為重新執行，因為那不會更換對外網路位址。
- GitHub 原生重新執行功能只能在當輪工作流程執行完畢後啟動；當輪所有抓取作業結束後，必須立即重跑失敗的作業，不得套用一般的兩小時等待規則，也不得等待每 15 分鐘執行一次的排程。
- 每一輪只能重跑失敗的抓取作業；已成功的抓取作業及其產出檔案必須保留，不得重跑。
- 同一次純文字抓取最多可自動重新執行 20 次；第 20 次重新執行仍然失敗後，才能停止並將任務標記為 `needs_attention`。
- 這個 20 次上限是指 GitHub 原生重新執行的次數，不包含最初的工作流程執行；因此最多是 1 次初始執行，加上 20 次重新執行。
- 修改抓取程式的重跑行為時，必須新增或更新防止問題再次發生的測試，至少涵蓋：當輪完成後立即重新執行、只重跑失敗的作業，以及 20 次上限。
